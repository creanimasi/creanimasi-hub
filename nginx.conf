server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location /api/hub {
        # Default nginx = 1 MB. Aplikasi TIDAK bergantung pada ini (PDF Riwayat diunggah per potongan ≤ 700 KB), dan di production
        # Coolify tidak memakai file ini — dibiarkan sebagai cadangan bila nginx dari repo ini dipakai.
        client_max_body_size 25m;
        proxy_pass http://163.61.44.177:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        internal;
    }
}
